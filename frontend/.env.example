NEXT_PUBLIC_API_URL=

# Secreto propio del frontend para firmar la cookie de sesión (jose/HS256).
# Distinto del JWT_SECRET del backend. Generar con:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=
